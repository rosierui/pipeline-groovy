pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '''#!/usr/bin/env groovy

                    def str = "Hello world"
                    def num =0

                    println str
                '''
            }
        }
    }
}
